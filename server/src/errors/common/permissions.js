const permissionsValidationErrors = {
    REQUIRED_PERMISSIONS: 'Select the permissions for the role',
    INVALID_PERMISSIONS: 'Select at least one permission'
};

export default permissionsValidationErrors;
