const durationMinValidationErrors = {
    REQUIRED_DURATION_MIN: 'Enter the minimum duration in hours',
    INVALID_DURATION_MIN: 'Minimum duration should be at least 1 hour'
};

module.exports = durationMinValidationErrors;